import "./Footer.css";

// Footer.js
// Componente simples de rodapé. Não recebe props atualmente, apenas renderiza
// conteúdo estático. Pode ser estendido no futuro para receber dados ou links.

function Footer() {
  return (
    <footer className="footer">
      <p>Projeto React - Atividade 1</p>
    </footer>
  );
}

export default Footer;